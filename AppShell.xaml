<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       x:Class="DTIapp.AppShell">

    <!-- Rotas para as páginas -->
    <ShellContent Title="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    <ShellContent Title="Load Items" ContentTemplate="{DataTemplate local:LoadItemsPage}" />
    <ShellContent Title="Create Clothes" ContentTemplate="{DataTemplate local:CreateClothesPage}" />
    <ShellContent Title="Schedule Clothes" ContentTemplate="{DataTemplate local:ScheduleClothesPage}" />
    <ShellContent Title="Statistics" ContentTemplate="{DataTemplate local:StatisticsPage}" />

</Shell>
